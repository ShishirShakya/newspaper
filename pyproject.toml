[project]
name = "conlaw"
version = "0.1.0"
description = "CONLAW project (e.g. Newspapers.com screenshot script)"
readme = "newspapers-prompt.md"
requires-python = ">=3.10"
dependencies = [
    "playwright>=1.49.0",
    "playwright-stealth>=2.0.0",
    "python-dotenv>=1.0.0",
]
